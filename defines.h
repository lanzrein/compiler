/**
 * @file defines.h
 * @brief file that contains a few structures and function to help the parsing of defines
 * */
 
 #define MAX_DEFINES 4096
 
 typedef struct {
	 char* identifier
	 char* arbitraryText
 }define;
 
 //~ typedef struct node{
	//~ define* def;
	//~ struct node* next;
 
 //~ };
 
 //~ int createNode(node* newNode,define* data, node* next);
 
 
 
 
 

 
 
 
